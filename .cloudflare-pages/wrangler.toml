name = "rate-my-advisor"
compatibility_date = "2024-01-01"
pages_build_output_dir = ".next"

[env.production]
name = "rate-my-advisor"

# Custom domain configuration
# Set via: wrangler pages domain add rate-my-advisor.com
# Or in Cloudflare Dashboard → Pages → Custom Domains
